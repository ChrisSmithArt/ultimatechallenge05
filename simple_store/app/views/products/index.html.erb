<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.title, product %>
      <br> 
      <small>Category: <%= product.category.name%></small>
    </li>
  <% end %>
</ul>
